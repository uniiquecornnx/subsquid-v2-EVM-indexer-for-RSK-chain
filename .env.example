# ---- Required ----
# Rootstock mainnet RPC (HTTPS) – replace with your provider
RSK_RPC_URL=https://public-node.rsk.co
# Or testnet:
# RSK_TESTNET_RPC_URL=https://public-node.testnet.rsk.co

# Start block (use a recent block to sync fast at first; change later if needed)
START_BLOCK=5600000

# Database (Subsquid spins up Postgres via `sqd up`)
DB_NAME=squid
DB_USER=squid
DB_PASS=squid
DB_HOST=localhost
DB_PORT=5432
